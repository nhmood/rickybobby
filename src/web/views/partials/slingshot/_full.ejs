<div class="row component user_info">
  <div class="user userA" data-id="<%= userA.data.id %>" data-type="user">

    <div class="row details">
      <div class="ten columns">
        <div class="row username">
          👋 Hey, <%= userA.data.username %>
        </div>
        <div class="row score">
          <%= summary.data.wins[userA.data.id] %>
          <% if(summary.data.winner[userA.data.id]){ %>
            🔥
          <% }; %>
        </div><!-- score -->
      </div>
      <div class="two columns">
        <a href="/users/<%= userA.data.username %>">
          <img class="profile u-full-width" src="<%= userA.data.image_url %>"></img>
        </a>
      </div>
    </div><!-- details -->
  </div><!-- userA -->

  <div class="row slingshot">
    Here are the rides that <strong><%= userB.data.username %></strong> has taken that you haven't!<br>
    Compete on these rides to slingshot past them!<br><br>
    Compare head to head with <a href="/shakeandbake?users=<%= userA.data.username %>,<%= userB.data.username %>">Shake and Bake!</a>
  </div>

  <div class="user userB" data-id="<%= userB.data.id %>" data-type="user">
    <div class="row details">
      <div class="two columns">
        <a href="/users/<%= userB.data.username %>">
          <img class="profile u-full-width" src="<%= userB.data.image_url %>"></img>
        </a>
      </div>
      <div class="ten columns">
        <div class="row username"><%= userB.data.username %></div>
        <div class="row score">
          <%= summary.data.wins[userB.data.id] %>
          <% if(summary.data.winner[userB.data.id]){ %>
            🔥
          <% }; %>
        </div><!-- score -->
      </div>
    </div><!-- details -->
  </div><!-- details -->
</div><!-- user_info -->
