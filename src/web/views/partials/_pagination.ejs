<% show = (prev_page || next_page) ? "" : "hide" %>

<div class="row pagination <%= show %>">
  <div class="six columns prev">
    <% if (prev_page){ %>
      <a href=<%= base %>?p=<%= prev_page %>>Previous <%= item %></a>
    <% } else { %>
      &nbsp;
    <% }; %>
  </div>


  <div class="six columns next">
    <% if (next_page) { %>
      <a href=<%= base %>?p=<%= next_page %>>Next <%= item %></a>
    <% } else { %>
      &nbsp;
    <% }; %>
  </div>
</div><!-- pagination -->
